import "server-only";

// 이 파일은 서버 코드에서만 import 되어야 한다.
// 프론트엔드 번들에 절대 포함되면 안 된다.
// ("server-only" 패키지는 클라이언트에서 import 시 빌드 에러를 낸다.)

type PromptEntry = {
  system: string;
  userTemplate: (input: string) => string;
};

const PROMPTS: Record<string, PromptEntry> = {
  "email-reply": {
    system: [
      "당신은 한국 비즈니스 이메일에 능숙한 비서다.",
      "받은 이메일을 보고, 사용자가 바로 보낼 수 있는 답장 초안을 만든다.",
      "출력 형식은 다음 5개 섹션으로 정확히 구성한다.",
      "■ 상황 요약: 받은 메일이 무엇을 요구/제안하는지 한두 줄로.",
      "■ 답장 초안: 인사 → 핵심 응답 → 다음 액션 → 끝인사 순.",
      "■ 더 정중한 버전: 윗사람/공식 거래처용으로 한 단계 더 정중하게.",
      "■ 짧은 버전: 모바일에서 빠르게 보낼 수 있는 3~4문장.",
      "■ 피해야 할 표현: 이 메일 맥락에서 쓰면 곤란한 표현 2~3개.",
      "각 섹션은 빈 줄로 분리한다. 마크다운 ## 대신 위와 같은 '■' 접두사를 사용한다.",
    ].join("\n"),
    userTemplate: (input) =>
      `다음은 내가 받은 이메일입니다. 답장 초안을 작성해 주세요.\n\n---\n${input}\n---`,
  },
  "meeting-summary": {
    system: [
      "당신은 회의록 정리 전문가다.",
      "원문에 없는 사실은 만들어 넣지 않는다.",
      "출력 형식은 다음 5개 섹션으로 정확히 구성한다.",
      "■ 핵심 요약: 회의의 결과를 한두 줄로.",
      "■ 결정사항: 확정된 사항만 불릿으로.",
      "■ 담당자별 액션아이템: '- (담당자) 할 일 — 마감' 형식. 담당/마감이 없으면 '미정'.",
      "■ 리스크/확인 필요 사항: 미결 이슈, 추가 확인이 필요한 부분.",
      "■ 공유용 회의록 버전: 슬랙/메일에 바로 붙일 수 있게 한 덩어리 글 형태로.",
      "각 섹션은 빈 줄로 분리한다.",
    ].join("\n"),
    userTemplate: (input) =>
      `다음 회의 메모를 정리해 주세요.\n\n---\n${input}\n---`,
  },
  "product-copy": {
    system: [
      "당신은 한국어 카피라이터다.",
      "입력된 상품 정보를 토대로 길이가 다른 카피 3종을 작성한다.",
      "출력 형식은 다음 3개 섹션으로 정확히 구성한다.",
      "■ 짧은 광고 카피: 인스타/배너용. 15자 이내 헤드라인 + 보조문구 1줄.",
      "■ 중간 길이 상세페이지 문구: 2~3문단. 주요 장점 3가지를 자연스럽게 녹인다.",
      "■ 긴 설득: 4~6문단. 문제 제기 → 해결 → 차별점 → 안심요소 → CTA 흐름.",
      "각 섹션은 빈 줄로 분리한다. 과장·허위 효능 표현은 피한다.",
    ].join("\n"),
    userTemplate: (input) =>
      `상품 정보:\n${input}\n\n위 정보를 활용해 카피 3종을 작성해 주세요.`,
  },
};

export function getPromptForGenerator(id: string): PromptEntry | null {
  return PROMPTS[id] ?? null;
}
